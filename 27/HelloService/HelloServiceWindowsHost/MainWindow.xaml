<Window x:Class="HelloServiceWindowsHost.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HelloServiceWindowsHost"
        mc:Ignorable="d"
        Title="HelloService - Windows host" MinHeight="100" MinWidth="300" SizeToContent="WidthAndHeight" Closed="Window_Closed">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition  />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition  />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition  />
        </Grid.ColumnDefinitions>
        <Button Name="btnRun" Grid.Row="0" Grid.Column="1" Content="Run" Margin="5" Click="btnRun_Click" />
        <Button Name="btnStop" Grid.Row="0" Grid.Column="3" Content="Stop" Margin="5,5,5,0" Height="23" VerticalAlignment="Top" Click="btnStop_Click" />
        <TextBlock Name="lblResult0" Grid.Row="1" Grid.ColumnSpan="5" Margin="5" />
    </Grid>
</Window>
